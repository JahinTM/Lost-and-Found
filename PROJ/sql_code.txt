
SQL*Plus: Release 21.0.0.0.0 - Production on Sun Oct 5 01:37:04 2025
Version 21.3.0.0.0

Copyright (c) 1982, 2021, Oracle.  All rights reserved.



-- Create user
CREATE USER c##sujana IDENTIFIED BY sujana123;

-- Grant privileges
GRANT CONNECT, RESOURCE TO c##sujana;

-- Allow the user to create tables, views, etc.
ALTER USER c##sujana QUOTA UNLIMITED ON USERS;

-- Optional but useful for development:
GRANT CREATE SESSION TO c##sujana;
GRANT CREATE TABLE TO c##sujana;
GRANT CREATE VIEW TO c##sujana;
GRANT CREATE SEQUENCE TO c##sujana;
GRANT CREATE TRIGGER TO c##sujana;
GRANT CREATE PROCEDURE TO c##sujana;
GRANT CREATE SYNONYM TO c##sujana;

-- If you want the user to have full access to everything (developer use only):
GRANT ALL PRIVILEGES TO c##sujana;




Enter user-name: c##sujana
Enter password:  sujana123
Last Successful login time: Sun Oct 05 2025 01:36:53 +06:00

Connected to:
Oracle Database 21c Express Edition Release 21.0.0.0.0 - Production
Version 21.3.0.0.0

SQL> CREATE TABLE Users (
    user_id         NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    user_name       VARCHAR2ARCHAR2(100) NOT NULL,
    std_id          VARCHAR2(20),
    user_type       VARCHAR2(20) CHECK (user_type IN ('Student', 'Admin')),
    user_password   VARCHAR2(50) NOT NULL
  );

Table created.


CREATE TABLE Report (
  2      report_id      NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  3      item           VARCHAR2(100) NOT NULL,
  4      report_date    DATE DEFAULT SYSDATE,
  5      reporter_id    VARCHAR2(20),   -- student ID
  6      floor_no       VARCHAR2(10),
  7      specific_place VARCHAR2(100)
  8  );

Table created.


SQL> CREATE TABLE Claim (
        claim_id     NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
        report_id    NUMBER,
        user_id      NUMBER,
        status       VARCHAR2(20) CHECK (status IN ('Pending', 'Approved', 'Rejected')),
        FOREIGN KEY (report_id) REFERENCES Report(report_id),
        FOREIGN KEY (user_id) REFERENCES Users(user_id)
    );

Table created.

SQL> commit;

Commit complete.

CREATE TABLE ContactMessages (
    message_id       NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    report_id        NUMBER NOT NULL,
    student_id       VARCHAR2(20) NOT NULL,
    contact_number   VARCHAR2(20) NOT NULL,
    details          VARCHAR2(500),
    message_date     DATE DEFAULT SYSDATE,
    status           VARCHAR2(20) DEFAULT 'Pending'  -- optional: Pending, Reviewed
);



//// Table FORMAT
SET LINESIZE 200
SET PAGESIZE 100
COLUMN EMAIL FORMAT A25
COLUMN FULL_NAME FORMAT A20
COLUMN DOB FORMAT A12